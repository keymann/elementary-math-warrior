/**
 * 모든 밸런스 수치는 이 파일 하나에만 둔다.
 *
 * 튜닝은 수십 번 반복되는 작업이라 값이 로직 사이에 흩어지면 조정이 불가능해진다.
 * (작업계획 4장의 목표 지표를 이 상수들로 맞춰 나간다)
 */

export const BALANCE = {
  world: {
    /** 충돌 그리드 셀 크기 — 최대 히트박스의 약 2배 */
    cellSize: 64,
    /** 한 판 길이(초) */
    runSeconds: 600,
  },

  player: {
    maxHp: 100,
    speed: 210, // 월드 단위/초
    radius: 16,
    /** 피격 후 무적 시간(초) — 없으면 적 무리에 닿는 순간 즉사한다 */
    invulnAfterHit: 0.7,
    /**
     * 보석 흡수 기본 반경.
     * 90 이었을 때, 넓게 도는 플레이(지나온 자리로 안 돌아감)에서 회수율이 크게 떨어졌다
     * (392킬에 Lv.4). 조작 스타일에 따른 성장 격차를 줄이려고 올렸다.
     *
     * 출제 빈도를 낮추려고 미믹 주기를 늘렸더니 **자석 아이템까지 같이 줄어** 레벨업이
     * 18.2 → 15.0 회로 떨어졌다. 미믹은 문항 공급원이자 XP 공급원이었다.
     * 둘을 분리한다 — 문항 수는 미믹 주기로, XP 는 기본 흡수 범위로 조절한다.
     */
    magnetRadius: 158,
    /**
     * 피격 시 밀려나는 속도. 숫자만 깎이면 맞았다는 게 손끝에 전달되지 않는다.
     * 무적 시간(0.7초) 안에 회복 가능한 크기로 둔다 — 밀려서 더 위험해지면 안 된다.
     */
    hurtKnockback: 230,
    /** 피격 비틀거림 지속(초) — 무적 시간보다 짧게 둬 조작 불능처럼 느껴지지 않게 한다 */
    hurtMotion: 0.42,
  },

  /**
   * 라운드(지형 구간)별 난이도.
   *
   * 시간에 비례하는 `hpScaleSeconds` 만으로는 경사가 완만해서, 후반에도 "그냥 더 오래
   * 하는 것"에 가까웠다. 지형이 바뀔 때마다 **눈에 띄는 계단**을 하나씩 놓는다.
   * 배경이 바뀌는 순간과 난이도가 오르는 순간이 일치해야 학생이 원인을 이해한다.
   *
   * 시간 배수 위에 곱해지므로 후반 배수가 급격해진다 — 용암 지대 기준
   * 체력 (1 + 480/200) × 1.42 ≈ 4.8배, 속도 ×1.18.
   */
  round: [
    { hp: 1, speed: 1, label: '숲' },
    { hp: 1.12, speed: 1.06, label: '사막' },
    { hp: 1.26, speed: 1.12, label: '현무암' },
    { hp: 1.42, speed: 1.18, label: '용암' },
    { hp: 1.6, speed: 1.24, label: '하늘' },
  ] as { hp: number; speed: number; label: string }[],

  enemy: {
    /** 서로 밀어내는 힘 — 겹쳐서 한 덩어리로 보이는 것을 막는다 */
    separation: 260,
    /** 플레이어에서 이 거리 밖으로 벗어나면 정리 */
    despawnDistance: 1800,
    /** 넉백 감쇠 (초당 남는 비율) */
    knockbackDamp: 0.02,
    /** 시간에 따른 체력 배수: 1 + t / hpScaleSeconds */
    hpScaleSeconds: 200,
    /**
     * 초반 접촉 피해 경사.
     * 시뮬레이션에서 미숙련 프로필의 첫 사망이 1분 43초로 목표(4~6분)에 한참 못 미쳤다.
     * 스폰을 줄이는 것만으로는 부족해서, 처음 2분간 피해량 자체를 낮춘다.
     * 후반 난이도는 그대로 두므로 숙련 플레이가 헐거워지지 않는다.
     */
    damageScaleAt: (t: number) => Math.min(1, 0.4 + t / 400),
  },

  spawn: {
    /**
     * 스폰 링 반경 — **반드시 화면 밖이어야 한다.**
     * 짧은 변 720 월드 기준 화면 반경은 16:10에서 679, 21:9 초광폭에서 914다.
     * 처음에 620~760으로 잡았더니 링이 화면 안으로 들어와 적이 눈앞에서 생성되고,
     * 이동 중인 플레이어가 갓 생긴 적에 그대로 박아 45초 만에 죽었다.
     */
    ringMin: 950,
    ringMax: 1120,
    /**
     * 동시 생존 상한. 성능 목표는 300체지만 **처음부터 300체를 띄우면 안 된다.**
     * 초반부터 화면이 가득 차면 피할 공간이 없어 즉사한다(측정에서 45초에 상한 도달).
     * 시간에 따라 열어 준다: 0분 70체 → 10분 300체.
     */
    maxAlive: 300,
    aliveCapAt: (t: number) => Math.min(300, 46 + (t / 60) * 26),
  },

  gem: {
    radius: 7,
    /** 자석 범위 안에서의 가속도 */
    pullAccel: 1400,
    maxSpeed: 900,
    /** 화면 밖 오래된 보석 정리 거리 */
    despawnDistance: 1600,
  },

  level: {
    /**
     * 레벨 L → L+1 에 필요한 경험치.
     * 목표: 10분 한 판에 15~20회 레벨업 (작업계획 4장).
     * 144판 시뮬레이션에서 21.2회가 나와 곡선을 약 15% 세웠다.
     * 출제 빈도를 늦추려고 여기서 한 번 더 올렸다가 레벨업이 14.8회로 떨어지고
     * 클리어율이 26.4%까지 내려가 되돌렸다. **출제 빈도는 보너스 주기로 조절한다** —
     * XP 곡선을 건드리면 성장 속도까지 같이 죽는다.
     */
    xpToNext: (level: number) => 6 + 6 * (level - 1) + Math.floor((level - 1) ** 1.78),

    /**
     * 몇 번째 레벨업마다 문제를 낼 것인가.
     *
     * 출제 빈도를 절반으로 낮추라는 요구를 **여기서** 해결한다.
     * XP 곡선을 세우는 방법은 이미 실패했다 — 문항 수와 함께 성장 속도까지 죽어
     * 클리어율이 26%까지 내려갔다. 레벨업 횟수는 그대로 두고 문제만 걸러 낸다.
     *
     * 2 면 1·3·5… 번째 레벨업에서만 출제한다. 첫 레벨업은 반드시 포함되도록
     * 홀수 쪽에 맞춘다 — 시작하자마자 한 번은 풀어 봐야 규칙을 안다.
     * 문제를 건너뛴 레벨업도 강화 카드는 그대로 준다. 성장이 끊기면 안 된다.
     */
    quizEveryLevels: 2,
  },

  special: {
    /** 미믹 — 보물상자인 척한다. 잡으면 보너스 문제 */
    star: { hp: 90, speed: 58, radius: 20, damage: 6, xp: 12 },
    /** 생선 도둑 고양이 — 도망친다. 잡으면 생선(회복) 드랍 */
    cat: { hp: 45, speed: 165, radius: 16, damage: 4, xp: 8, fleeRadius: 260 },
    /** 생선 회복량 */
    fishHeal: 50,
    /** 폭탄 아이템 피해 */
    bombDamage: 400,
    /** 아이템 획득 반경 */
    pickupRadius: 34,
  },

  boss: {
    /** 보스 등장 위치 — 스폰 링보다 조금 가깝게 */
    spawnDistance: 520,
    /** 보스전 동안 일반 적 스폰 비율 */
    spawnScaleDuringBoss: 0.3,
    /**
     * 중간보스 제한 시간(초). 넘으면 보스가 물러나고 타이머가 다시 흐른다.
     * 없으면 보스를 못 잡는 플레이어의 판이 영원히 끝나지 않는다.
     */
    stallSeconds: 100,
  },

  transcend: {
    /** 초월 시 배수 */
    power: 1.35,
    rate: 1.25,
    /** 초월 수련 문제 수 */
    trialQuestions: 3,
  },

  /**
   * 지형별 게임플레이 변화.
   *
   * 배경만 바뀌고 조작감이 그대로면 "그림이 바뀌었네" 로 끝난다. 구간마다 손끝에서
   * 다르게 느껴져야 진행감이 생긴다. 다만 **초등 대상이라 벌점형 효과는 피한다** —
   * 느려지기만 하는 지형은 재미가 아니라 스트레스다. 그래서 잃는 게 있으면 얻는 것도 준다.
   */
  biome: {
    // 숲 — 기준
    forest: { player: 1, enemy: 1, accel: 26, gem: 1, label: '' },
    // 모래에 발이 빠진다. 적도 같이 느려져 억울하지 않다
    desert: { player: 0.9, enemy: 0.88, accel: 26, gem: 1, label: '모래에 발이 푹푹 빠진다' },
    // 단단한 현무암 — 발이 잘 붙는다. 대신 적도 거침없이 달려온다
    basalt: { player: 1.05, enemy: 1.06, accel: 30, gem: 1, label: '단단한 바위, 발이 잘 붙는다' },
    // 뜨거워서 오래 서 있을 수 없다 — 양쪽 다 빨라져 긴박해진다
    lava: { player: 1.1, enemy: 1.14, accel: 26, gem: 1, label: '뜨겁다! 멈추지 말고 달려' },
    // 구름 위 — 미끄러진다. 대신 보석이 멀리서도 끌려온다
    sky: { player: 1.08, enemy: 1, accel: 4.2, gem: 1.45, label: '구름 위라 미끄러워!' },
  } as Record<string, { player: number; enemy: number; accel: number; gem: number; label: string }>,

  /**
   * 발자국 자국 — 플레이어가 지나간 자리의 지면이 부서진다.
   *
   * 밟은 즉시 최대 진하기로 튀어나오면 "게임이 깜빡인다"로 보인다.
   * 짧게 번지고(fadeIn) 오래 남았다가(hold) 천천히 사라지게(fadeOut) 한다.
   */
  decal: {
    /** 자국 하나를 남기는 데 필요한 이동 거리(월드 단위) */
    everyDistance: 27,
    /** 동시 유지 개수 — 넘으면 가장 오래된 것부터 덮어쓴다 */
    max: 150,
    fadeIn: 0.18,
    hold: 2.6,
    fadeOut: 5.5,
  },

  perf: {
    /** 이 fps 아래가 lowFpsSeconds 이상 지속되면 저사양 모드로 자동 전환 */
    lowFpsThreshold: 45,
    lowFpsSeconds: 3,
  },
} as const;
